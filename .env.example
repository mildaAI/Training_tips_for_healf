# Example environment variables for the Streamlit Ollama app
# Set OLLAMA_HOST if Ollama is served on a non-default host/port
OLLAMA_HOST=http://localhost:11434
# If your Ollama instance uses API keys, set OLLAMA_API_KEY (app reads only when used by the Ollama client)
# OLLAMA_API_KEY=your_api_key_here
